{
  "name": "diffscope-renderer",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.js --bundle --format=iife --outfile=../Sources/diffscope-app/Renderer/renderer.js --minify && esbuild src/terminal.js --bundle --format=iife --outfile=../Sources/diffscope-app/Renderer/terminal.js --minify && cp src/index.html src/terminal.html src/tokens.css ../Sources/diffscope-app/Renderer/ && node hash-sources.mjs"
  },
  "dependencies": {
    "@codemirror/lang-javascript": "^6.2.4",
    "@codemirror/state": "^6.5.2",
    "@codemirror/view": "^6.43.6",
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
