# eslint-autofix

Autofix churn: semicolons added, an array spaced, a loop given braces, two declarations swapped. Several unrelated formatting edits in one file, which is what the reader actually meets after a lint run.
