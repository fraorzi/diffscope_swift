# spread-prop-reordering

`{...defaults}` moved relative to `value`, which can change which value wins. §4.1 asks for it to be classified as potentially behaviour-affecting; it is not, for the same reason as `prop-reordering`, and the same gap is recorded. What is asserted is that it is never presented as formatting-only.
