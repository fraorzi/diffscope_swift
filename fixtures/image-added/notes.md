# image-added

No left side. Blend, Split and Pixel diff have nothing to compare against, and each is **disabled
with that reason beside it** rather than hidden — the same rule an unavailable scope follows
(`12-…` §3).

`before.png` is deliberately empty rather than absent: the corpus stores pairs, and an empty side
is how "this did not exist" is written down.
