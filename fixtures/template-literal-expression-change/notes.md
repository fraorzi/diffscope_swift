# template-literal-expression-change

An edit inside `${...}` rather than in the literal text beside it. Two bytes, inside a template literal, which tree-sitter models as several nodes rather than one string.
