# duplicated-nodes

Repeated identical siblings: which one was added is genuinely undecidable (OQ-027).
Confidence must drop rather than a guess being presented as fact. DEC-045 keeps the
detection and shows no indicator, so this fixture guards the anchor selection, not a badge.
