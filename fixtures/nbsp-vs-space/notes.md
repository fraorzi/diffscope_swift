# nbsp-vs-space

A non-breaking space where an ordinary space was. Both formatting AND invisible —
the two disclosure axes cross here, which is why DEC-023 makes them separate axes
rather than one vocabulary.
