# svg-rendered-change

The inner square moves by two units. Source and rendering both differ, so the two readings must
agree that something changed — this is the control for `svg-text-only-change`, which is the same
file with only the source moving.

A comparison that reported "renders identically" here would be the failure F18's sentence is meant
to prevent, arriving from the other direction.
