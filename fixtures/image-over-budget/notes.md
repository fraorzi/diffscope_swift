# image-over-budget

5000 × 4000 is 20 megapixels against a budget of 16 (`RenderedComparison.megapixelBudget`).

Pixel diff is unavailable and says why; both renderings are still shown, because the budget is a
statement about what was *measured*, not about what can be looked at. Uniform content, so the file
stays under 90 KB — what is under test is the refusal, not the disk.
