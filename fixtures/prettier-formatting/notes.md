# prettier-formatting

Whole-object reformat with no semantic change, plus the trailing comma prettier adds.
Grouped, never hidden, with the count disclosed (DEC-017).
Note the line counts differ, so DEC-048 declines to group it — the conservative direction.
