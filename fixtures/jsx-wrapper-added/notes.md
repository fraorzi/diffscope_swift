# jsx-wrapper-added

The inverse of the founding case: a fragment gains a `<div className="page">`. Worth having separately, because the two directions are not symmetric for the alignment — the added side has bytes with nothing to match against, where the removed side has bytes with nothing to match *to*.
