# zero-width-characters

A zero-width space inserted inside an identifier-looking string. The two sides
render identically and differ in bytes, so the difference must be reported (INV-3)
and disclosed as invisible (DEC-023), naming the codepoint in Expanded.
